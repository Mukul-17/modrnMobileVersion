# ─── Local dev helper (not part of the site) ───
# server.js is just the local preview server. Remove this line if you
# actually want to commit/share it.
server.js

# ─── OS junk ───
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ─── Editor / IDE ───
.vscode/
.idea/
*.sublime-workspace
*.swp
*~

# ─── Node (only if you ever add tooling here) ───
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# ─── Logs & misc ───
*.log

# ─── Local backups (revert snapshots, not part of the site) ───
_backup/
